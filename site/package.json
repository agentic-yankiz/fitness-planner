{
  "name": "workout-program-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static-site generator for Shaked's workout plan (light, local, Tailscale-served).",
  "scripts": {
    "build": "node build.mjs",
    "start": "node server.mjs",
    "migrate": "node migrate.mjs",
    "backup": "node bin/backup-db.mjs",
    "test": "node --test",
    "export:tracking": "node scripts/export-tracking.mjs",
    "backfill:history": "node scripts/backfill-history.mjs",
    "dev": "BASE_PATH=/fitness node build.mjs && (SERVER_PORT=3000 node server.mjs &) && SITE_DIST=$(pwd)/dist FITNESS_SERVER_PORT=3000 caddy run --config Caddyfile --adapter caddyfile",
    "install:launchd": "./bin/install-local-launchd.sh"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
